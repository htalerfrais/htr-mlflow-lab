experiment_name: "Local TrOCR with Preprocessing"
run_name: "trocr-local-test-with-preprocessing"
pipeline: "line_to_text"
dataset: "local_lines"
model: "trocr"
preprocessor: "identity"
# Alternative: use a list for sequential preprocessing
# preprocessor: ["identity", "binary"]

